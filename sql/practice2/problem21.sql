-- 문제 21
-- student 테이블에는 학과(major)와 GPA가
-- 있습니다.
-- 학과별·GPA 구간별 인원을 집계하되, 소계(소계
-- 행)와 총계 행까지 한 번의 쿼리로 아래의 내용이
-- 반영되도록 출력
-- - GPA를 구간
--   (3.0 미만 / 3.0~3.5 / 3.5 초과)으로
--   분류하는 파생 컬럼을 추가
-- - GROUP BY ROLLUP(major, gpa_tier)로
--   학과별, 전체 소계를 동시에 조회
-- - GROUPING(major) 함수로 소계 행에
--   '전체' 라벨을 붙일 것
-- - 결과를 major, gpa_tier 순으로 정렬하되
--   소계 행은 하단에 표시

